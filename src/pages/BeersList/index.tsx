import { Heading, VStack } from "@chakra-ui/react";

const BeersList = () => {
  return (
    <VStack spacing={8}>
      <Heading>Beers List</Heading>
    </VStack>
  );
};

export default BeersList;
